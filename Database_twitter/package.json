{
  "devDependencies": {
    "prisma": "^5.3.1"
  },
  "dependencies": {
    "@prisma/client": "^5.3.1",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "nodemon": "^3.0.1"
  },
  "scripts": {
    "dev": "node app.js",
    "production:build": "npx prisma generate && npx prisma migrate deploy"
  }
}
